export default function Footer(){
  return (
    <footer className="footer">
      <div className="container">
        © {new Date().getFullYear()} NutriVision — Eat well, move daily, sleep deeply.
      </div>
    </footer>
  );
}
